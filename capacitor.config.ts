import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.edgartesting.app',
  appName: 'code-scanner-mobile-app',
  webDir: 'dist/code-scanner-mobile-app'
};

export default config;
